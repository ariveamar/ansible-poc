---
- name: Deny Firewall Rule
  gather_facts: no
  hosts: all
  tasks:
    - name: Deny HTTP on Firewalld
      ansible.posix.firewalld:
        service: http
        permanent: yes
        state: disabled
